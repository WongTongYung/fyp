         Once the raw video footage is captured, it is processed into individual frames using the
OpenCV library. These frames were manually annotated using tools of Roboflow, where bounding
boxes were drawn around the pickleball in each frame to clearly define the object of interest.
Preliminary validation of this annotation workflow was conducted using proxy objects, as shown
in Figure 3.4, before proceeding to full-scale on-court data collection.

     Figure 3.4 Illustration of the bounding box annotation workflow using a proxy object for
                                               preliminary testing

         Finally, the annotated dataset was partitioned into three distinct subsets: Training Set of 70%
for learning features, Validation Set of 20% for hyperparameter tuning during training, and Testing
Set of 10% for the final unbiased evaluation.

                                                         28
3.2.2 Phase 2: Core Detection Module (Yolov11 Training)
         In this phase, the training process focuses on maximizing the Mean Average Precision

(mAP). The model is fine-tuned on the custom dataset prepared in Phase 1 using the PyTorch deep
learning framework. mAP is a performance metric that calculates the average precision across
various recall levels, providing a single score that balances false positives and false negatives. This
metric is crucial for validating the model's consistency in detecting the small, high-velocity
pickleball across different frames. Specifically, the system aims to optimize mAP@50, and
hyperparameters will be iteratively adjusted to maximize this score, ensuring high-confidence
tracking in real-time.

3.2.3 Phase 3: Game Logic And Database Development
         A python language is used to construct a base game logic engine as a state machine. This

engine is responsible for enforcing the specific rules of pickleball, particularly the traditional side-
out scoring format, which requires tracking the serving teams score, the receiving teams score, and
the server number. Concurrently, a local storage solution is implemented using the SQLite database
engine. This database is designed to log game events accompanied by a precise timestamp. This
ensures the system works entirely offline and preserves match data for post-game analysis.

3.2.4 Phase 4: System Integration And Testing
         The final phase involves integrating the independent components into a unified functional

prototype. The coordinates generated by the YOLOv11 module in phase 2 are fed directly into the

                                                         29
game logic engine developed in phase 3. The engine interprets these coordinates to update the game
state. Consequently, a user dashboard is developed using standard web tools to display the live
score and match status. The completed system then undergoes validation testing, where the
automated point counting accuracy is compared against manual scorekeeping to evaluate its
effectiveness and reliability in a real-world scenario.

3.3 Requirements Analysis
         This section classifies the system specifications into two categories which are functional

requirements and non-functional requirements. The functional requirements are the core operations
of the system. Non-Functional Requirements specify the quality attributes. These requirements
serve as the validation criteria for the final prototype. To visualize the structural hierarchy and
grouping of these requirements, the system modeling is illustrated in Figure 3.5.

                                                         30
Figure 3.5 System Modeling Language Diagram
                           31
3.3.1 Functional Requirements
         Functional requirements define the specific behaviors, operations, and functions that the

system must execute to satisfy the project objectives. These requirements have been derived from
the project scope and the official rules of pickleball side-out scoring. Table 3.1 shows the functional
requirements list of the project.

               Table 3.1 Table 3.1 List of Functional Requirements

ID  Use Case   Requirement Description                              Priority

FR-01 Capture Video The system shall accept a continuous live video stream High

    Stream     from a standard USB webcam.

FR-02 Capture Video The system shall process the raw video stream by High

    Stream     extracting individual frames in real-time.

FR-03 Process Object The system shall utilize the YOLOv11-m deep High

    Detection  learning model to detect the pickleball in video

               frames.

FR-04 Process Object The system shall output the geometric coordinates of High

    Detection  the detected ball for every frame.

FR-05 Update Game The system shall implement a Python-based logic High

    State      engine to interpret the coordinates of the ball.

FR-06 Update Game The system shall automatically calculate the score High

    State      using traditional side-out scoring rules.

FR-07 Update Game The system shall track the three-number score     High

    State

                        32
FR-08 Save Match The system shall utilize a local SQLite database to Medium

Log              store game data and logs.

FR-09 Save Match The system shall generate a timestamped log for every Medium

Log              significant game event.

FR-10 View Live  The system shall provide a graphical dashboard Medium

Dashboard        interface viewable in a standard web browser.

FR-11 View Live  The dashboard shall display the live match score to the High

Dashboard        users in real-time.

FR-12 Rewind Video The dashboard shall include a video rewind feature to Low

                 replay recent footage for verification.

                                      33
